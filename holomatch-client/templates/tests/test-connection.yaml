apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "holomatch-client.fullname" . }}-test-connection"
  labels:
    {{- include "holomatch-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "holomatch-client.fullname" . }}-desktop:{{ .Values.desktop.service.port }}']
  restartPolicy: Never
