{{- if .Values.desktop.ingress.enabled -}}
{{- $fullName := include "holomatch-client.fullname" . -}}
{{- $svcPort := .Values.desktop.service.port -}}
{{- if and .Values.desktop.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.desktop.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.desktop.ingress.annotations "kubernetes.io/ingress.class" .Values.desktop.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-desktop
  labels:
    {{- include "holomatch-client.labels" . | nindent 4 }}
  {{- with .Values.desktop.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.desktop.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.desktop.ingress.className }}
  {{- end }}
  {{- if .Values.desktop.ingress.tls }}
  tls:
    {{- range .Values.desktop.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.desktop.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-desktop
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-desktop
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
